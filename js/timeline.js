/* ---------------------------------------------------------------------------
   ЕДИНСТВЕННЫЙ ФАЙЛ, КОТОРЫЙ НУЖНО ПРАВИТЬ ДЛЯ НАСТРОЙКИ ПРОТОТИПА.
   Здесь: путь к видео, скорость скролла и список «остановок» с контентом.
   --------------------------------------------------------------------------- */

window.TIMELINE = {

  video: {
    // Путь к видео. Кладём сюда результат scripts/encode-video.sh
    src: 'assets/video/main.mp4',

    // Длительность в секундах. Нужна только как запасное значение до того,
    // как браузер прочитает метаданные (и чтобы страница работала без видео).
    // Реальная длительность из файла всегда важнее — правится автоматически.
    fallbackDuration: 17
  },

  scroll: {
    // Сколько экранов скролла тратится на 1 секунду видео.
    // Больше — медленнее и плавнее, длиннее страница. 0.6–1.2 — рабочий диапазон.
    screensPerSecond: 0.9,

    // Сглаживание перемотки: 0.05 — очень плавно и «с инерцией», 1 — жёстко за скроллом.
    smoothing: 0.14,

    // Пустой скролл после конца видео, в экранах.
    endPad: 0.5,

    // Доля отрезка остановки, которая уходит на появление / исчезновение контента.
    fade: 0.28
  },

  /* -------------------------------------------------------------------------
     ОСТАНОВКИ.
     at    — секунда видео, на которой замирает картинка
     hold  — длина паузы в экранах скролла (1 = один полный экран прокрутки)
     image — путь к ассету из assets/images/
     align — center | left | right | bottom   (положение блока на экране)
     width — ширина блока в px (по умолчанию 720)
     title / text — необязательный текст, если он не вшит в картинку

     Пока это демо-точки без картинок — чтобы механику можно было потрогать
     сразу. Заменим на реальные, когда придут ассеты и тайминги.
     ------------------------------------------------------------------------- */
  stops: [
    { at: 3,  hold: 1.2, image: null, align: 'center', title: 'Остановка 1', text: 'Здесь будет первый ассет. Видео стоит на 3-й секунде.' },
    { at: 8,  hold: 1.2, image: null, align: 'left',   title: 'Остановка 2', text: 'Второй блок. Видео стоит на 8-й секунде.' },
    { at: 13, hold: 1.2, image: null, align: 'right',  title: 'Остановка 3', text: 'Третий блок. Видео стоит на 13-й секунде.' }
  ]
};
