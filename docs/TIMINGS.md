# Тайминги

Ролик: 25.09 с, 1440×900, 29.97 к/с — ровно под кадр макета и под базу
таймкода.

Тайминги размечены в 30 кадрах в секунду: `16_20` = 16 с + 20/30 = 16.67 с.
`video.fps: 30` в конфиге отвечает именно за это.

## Раскладка

| # | Ассет | Интервал | Секунды | Что в кадре |
|---|---|---|---|---|
| 1 | framework | `00_00 – 03_00` | 0.00 – 3.00 | самолёт у скалы |
| 2 | course | `05_00 – 06_00` | 5.00 – 6.00 | пилот в кабине |
| 3 | book | `07_20 – 09_00` | 7.67 – 9.00 | приборная панель, книга на торпеде |
| 4 | reviews | `11_20 – 13_00` | 11.67 – 13.00 | карта-цель |
| 5 | conference | `14_00 – 15_05` | 14.00 – 15.17 | гоняющие машины |
| 6 | clothes | `16_20 – 18_16` | 16.67 – 18.53 | ровный градиент в лайм |
| 7 | about | `20_06 – 26_00` | 20.20 – 25.09 | стадион, гонщик в кадре |

`00. static header` в таблице нет: шапка лежит поверх всего и видна всегда.

Интервал седьмой остановки длиннее ролика — движок подрезает его по концу
видео, и блок держится до самого низа страницы.

Вся страница — примерно 21 экран прокрутки.

## Границы сцен

| Секунды | Сцена |
|---|---|
| 0.0 – 4.2 | скала, самолёт уходит |
| 4.2 – 5.0 | облака |
| 5.0 – 7.0 | пилот в кабине, палец вверх |
| 7.2 – 9.8 | приборная панель с книгой |
| 10.5 – 14.0 | карта-цель |
| 14.3 – 16.3 | гоняющие машины |
| **16.6 – 19.8** | **ровный градиент в лайм — фон под галерею одежды** |
| 20.0 – 21.3 | клетчатый флаг |
| 21.5 – 25.09 | стадион, пилот у самолёта |

Тайминги совпадают со сценами кадр в кадр — это те же цифры, что были
размечены изначально.

## История правок

Ролик за последние недели присылали в разных версиях, и часть из них
сдвигала сцены относительно разметки. Текущая версия вернулась к исходной
раскладке, поэтому тайминги стоят ровно те, что были заданы с самого начала.

Единственное, что в ролике поменялось по содержанию: на интервале одежды
раньше были трибуны с толпой, теперь — ровный градиент в лайм с сеткой,
специально как фон под галерею вещей.

## Как снять новый тайминг

1. `node scripts/serve.js`, открыть прототип.
2. `D` — панель с текущим таймкодом в формате `СС_КК` при 30 к/с.
3. Доскроллить до нужного кадра, `T` — таймкод запишется.
4. Перенести в `js/timeline.js`.

`H` прячет ассеты и шапку — удобно, когда надо разглядеть само видео.

## Что покрутить, если ощущается не так

| Ощущение | Что менять в `js/timeline.js` |
|---|---|
| Слишком долго листать между блоками | `scroll.screensPerSecond` меньше |
| Пустой скролл перед конкретным блоком | `lead` у этой остановки |
| Ассет мелькает, не успеваешь прочитать | `scroll.pinScreens` больше |
| Ассет появляется слишком резко | `scroll.fadeScreens` больше |
| Хочется проезд снизу вместо проявления | `scroll.reveal: 'slide'` |
| Видео «плывёт» за скроллом с задержкой | `scroll.smoothing` ближе к 1 |
| Видео дёргается на быстром скролле | `scroll.smoothing` меньше |
| Под ассетом видео не должно двигаться совсем | `freeze: true` у остановки |
